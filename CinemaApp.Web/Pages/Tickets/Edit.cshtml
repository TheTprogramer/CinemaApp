@page "{id:int}"
@model CinemaApp.Web.Pages.Tickets.EditModel
@{ ViewData["Title"] = "Редактирай билет"; }

<div class="row justify-content-center">
  <div class="col-md-4">
    <div class="card shadow-sm border-0">
      <div class="card-header bg-warning text-dark">
        <h5 class="mb-0"><i class="bi bi-pencil me-2"></i>Редактирай билет</h5>
      </div>
      <div class="card-body">
        <form method="post">
          <input asp-for="Ticket.TicketId" type="hidden" />
          <div class="mb-3">
            <label asp-for="Ticket.TicketPrice" class="form-label">Цена (лв.)</label>
            <input asp-for="Ticket.TicketPrice" class="form-control" type="number" step="0.5" />
            <span asp-validation-for="Ticket.TicketPrice" class="text-danger small"></span>
          </div>
          <div class="d-flex gap-2">
            <button type="submit" class="btn btn-warning"><i class="bi bi-save me-1"></i>Запази</button>
            <a href="/Tickets" class="btn btn-outline-secondary">Отказ</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
